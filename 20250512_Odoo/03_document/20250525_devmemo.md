# 20250525开发纪要

## 已知问题

1. contact 表中，"industry", "Salesperson", "Trading Partner ID"均为空，后期会影响分析维度；
2. product 表中，缺少product id，无法通过name字段作为主键，有重复记录。目前只能通过sku与sales oorder line表中的 “Product”字段进行匹配。
3. salesorder 表中缺少sales order number，目前使用"Order Reference"字段代替；
4. salesorderline表中，缺少sales order line number, 多个字段如“CF RMA Unit Serial Number”，“CF Serial Number[1-10]”， “CF-ShipExpress SO Return Tracking”为空，表中product名称与product表的name信息无法匹配；

## 模型设计

![sales model](./model_2025-05-25%20174243.png)


## DAX设计

1. 总销售额 order_sales

``` sql
order_sales = CALCULATE(SUM(salesorder[Total]))
```

2. 总销售额 order_line_sales

``` sql
order_line_sales = CALCULATE(sum(salesorderline[Total]))
```

3. 订单量 order_cnt

``` sql
order_cnt = CALCULATE(DISTINCTCOUNTNOBLANK(salesorder[Order Reference]))
```

4. 订单行项目量 order_line_cnt

``` sql
order_line_cnt = CALCULATE(DISTINCTCOUNTNOBLANK(salesorderline[Order Reference]))
```


## 报告设计

![报告设计](./report_2025-05-25%20175834.png)

## 总结

- 因产品、客户维度数据质量较差，导致事实数据salesorder数据与其他维度数据关联后无法通过产品、地区（客户维度中的国家、城市等）进行分析。

- 数据上仅有2025年5月的数据，无法体现环比、同比等计算项目；